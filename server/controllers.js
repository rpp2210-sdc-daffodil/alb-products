/* eslint-disable camelcase */
const getAllProducts = () => {
  // query database for complete list of products
  // do some research regarding the page and count parameters --> especially pages
};

const getSpecificProduct = (product_id) => {
  // use given product_id to query database
  // shape data into desired formate
};

const getStylesForProduct = (product_id) => {
  // use given product_id to query database and return styles information
};

const getRelatedProducts = (product_id) => {
  // use product_id to retrieve related information
  // NOTE: this is where optimization may need to be done to the data as stored
};
